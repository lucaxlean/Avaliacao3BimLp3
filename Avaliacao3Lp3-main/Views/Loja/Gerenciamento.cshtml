@model List<AvaliacaoLp3.Models.LojaViewModel>

    @{
        ViewData["Title"] = "Gerenciamento";
    }
    
    <h1>Gerenciamento</h1>
    
    @if(Model.Count == 0)
    {
        <p>Não há nenhuma loja cadastrada</p>
    }
    else
    {
        <hr>
        @foreach(var loja in Model)
        {
            <h3>@loja.Nome</h3>
            <a href="Detalhes/@loja.Id">Detalhe</a>
            <a href="Excluir/@loja.Id">Excluir</a>
            <hr>
        }
    }